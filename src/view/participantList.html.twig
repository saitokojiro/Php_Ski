{% extends 'template.html.twig' %}

{% block title %}
	Home
{% endblock %}

{% block content %}
	<div class="display">
		<div class="btn-ctl">create new event</div>
		<div class="btn-ctl">List event</div>

{% for data in datas %}
			<span>{{ data.participant_id}}</span>
			<span>{{ data.participant_nom}}</span>
			<span>{{ data.participant_prenom}}</span>
		{% endfor %}
	</div>
{% endblock %}
